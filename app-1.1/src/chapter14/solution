a.
int sum = 0;
for (int n = N; n > 0; n /= 2)
  for(int i = 0; i < n; i++)
    sum++;

b.
int sum = 0;
for (int i = 1 i < N; i *= 2)
  for (int j = 0; j < i; j++)
    sum++;

Порядок роста этой программы NlgN, т.к. порядок роста внутреннего цикла N, порядок внешнего цикла lgN.